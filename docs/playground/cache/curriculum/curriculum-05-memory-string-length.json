{
  "schemaVersion": 1,
  "success": true,
  "version": "0.2.1-alpha",
  "source": "D:\\_2025\\Gits\\megaalive\\hlax64\\examples\\curriculum\\05-memory\\string-length.hla64",
  "target": "linux-x64-sysv",
  "diagnostics": [],
  "ir": [
    "function _start:\n  entry:\n    Call  [StrLen]\n    v2 = Move v3",
    "function StrLen:\n  entry:\n    v0 = Move v1\n    Call v0 [extern:hlax_strlen]"
  ],
  "lowered": [
    "function _start:\n  entry: True, export: False\n  stack frame: 8 bytes\n  entry:\n        xor ebx, ebx    ; default exit code = 0 (callee-saved)\n        push rbp\n        mov rbp, rsp\n        sub rsp, 8      ; align stack to 16 bytes\n        sub rsp, 8      ; align stack\r\n    call StrLen\r\n    add rsp, 8      ; restore stack\n        mov rbx, rax\n        mov rdi, rbx    ; exit code\n        mov rax, 60     ; sys_exit\n        syscall",
    "function StrLen:\n  entry: False, export: False\n  stack frame: 0 bytes\n  externs: hlax_strlen\n  entry:\n        push rbp\n        mov rbp, rsp\n        lea rcx, [rel str_0]\n        mov rdi, rcx\r\n    sub rsp, 8      ; align stack\r\n    call hlax_strlen\r\n    add rsp, 8      ; restore stack\n        pop rbp\n        ret"
  ],
  "nasm": "bits 64\r\nextern hlax_strlen\r\nsection .text\r\nglobal _start\r\n\r\n_start:\r\n    xor ebx, ebx    ; default exit code = 0 (callee-saved)\r\n    push rbp\r\n    mov rbp, rsp\r\n    sub rsp, 8      ; align stack to 16 bytes\r\n    sub rsp, 8      ; align stack\r\n    call StrLen\r\n    add rsp, 8      ; restore stack\r\n    mov rbx, rax\r\n    mov rdi, rbx    ; exit code\r\n    mov rax, 60     ; sys_exit\r\n    syscall\r\n\r\nStrLen:\r\n    push rbp\r\n    mov rbp, rsp\r\n    lea rcx, [rel str_0]\r\n    mov rdi, rcx\r\n    sub rsp, 8      ; align stack\r\n    call hlax_strlen\r\n    add rsp, 8      ; restore stack\r\n    pop rbp\r\n    ret\r\n\r\nsection .data\r\nnewline db 0x0A\r\nstr_0 db \u0022hello\u0022, 0\r\n"
}
