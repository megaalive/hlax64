program stack_align;

#include("stdlib64.hhf")

procedure AddFive(x:int64); @returns("rax");
begin AddFive;
    mov(x, rax);
    add(5, rax);
end AddFive;

begin stack_align;
    mov(10, rax);
    call AddFive(rax);
    stdout.put("10 + 5 = ", rax, nl);
end stack_align;
