# Arithmetic

| Example | Description |
|---------|-------------|
| [simple.hla64](simple.hla64) | Basic `mov` and `add` on `rax` |
| [const-expr.hla64](const-expr.hla64) | Compile-time `const` folding (`Width * Height`) |
| [expr-assign.hla64](expr-assign.hla64) | Runtime `:=` expressions with const names and bitwise mask |

Expected exit code: `3` (1 + 2).
