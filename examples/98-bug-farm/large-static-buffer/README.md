# large-static-buffer

4096-byte `.bss` static array walked with indexed byte loads. Stresses BSS sizing and `[symbol + index]` addressing.

Expected exit code: **0** (compile-only curriculum manifest).
