# many-externs

Seven `kernel32.dll` extern declarations with sequential calls. Stresses import metadata emission and Win64 call lowering for multiple foreign symbols in one unit.

Expected exit code: **0** (compile-only curriculum manifest).
