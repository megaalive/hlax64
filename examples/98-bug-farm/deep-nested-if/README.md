# deep-nested-if

Six levels of nested `if`/`else` inside `Classify`. Stresses CFG block ordering and merge layout (similar to real-tool `cmp` / `filemagic` patterns).

Expected exit code: **0** (compile-only curriculum manifest).
