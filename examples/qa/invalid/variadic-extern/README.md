# variadic-extern

Declares variadic `printf` and calls it with a `float64` argument — not supported yet. The compiler should report **HLAX0055**.

Mirrors `tests/conformance/invalid/variadic-extern/`.
