# nested-while

Nested `while` loops with distinct induction variables (`r8`/`r9` outer/inner). Stresses CFG block ordering and loop header layout without procedure calls.

Expected exit code: **0** (compile-only curriculum manifest).
