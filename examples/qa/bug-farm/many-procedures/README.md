# many-procedures

Ten tiny procedures (`P0`–`P9`) called in sequence from `main`. Stresses symbol emission, call lowering, and accumulator live range across many call sites.

Expected exit code: **0** (compile-only curriculum manifest).
