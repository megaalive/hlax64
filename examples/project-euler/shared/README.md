# Shared snippets

HlaX64 does not preprocess `#include` for local `.hla64` files yet. Copy routines from [`../docs/patterns.md`](../docs/patterns.md) into your problem or runner.

Planned files when include lands:

- `math.hla64` — `SumMultiplesBelow`, divisibility helpers
- `primes.hla64` — trial division, sieve sketches
- `fibonacci.hla64` — iterative Fibonacci step
- `digits.hla64` — parse argv number, decimal digits

For now, see **patterns.md** for copy-paste blocks.
