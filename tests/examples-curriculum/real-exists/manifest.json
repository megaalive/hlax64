{
  "name": "real-exists",
  "source": "../../../examples/tools/10-windows/exists/exists.hla64",
  "expectedExitCode": 0,
  "description": "Real tool: check whether a fixture file exists (compile-only until Windows manifest execution lands)"
}
