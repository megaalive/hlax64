// add_two.hla64
// Demonstrates a procedure call with the Linux x64 System V ABI.

program main;

procedure AddTwo(a:int64; b:int64); @returns("rax");
begin AddTwo;
    mov(a, rax);
    add(b, rax);
end AddTwo;

begin main;
    call AddTwo(10, 20);
    stdout.put("10 + 20 = ", rax, nl);
end main;
