program pointer_load_store;

procedure LoadViaPtr; @returns("rax");
var
    slot: int64;
begin LoadViaPtr;
    mov(99, slot);
    mov(&slot, rcx);
    mov([rcx], rax);
end LoadViaPtr;

begin pointer_load_store;
    call LoadViaPtr();
end pointer_load_store;
