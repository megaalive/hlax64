#!/bin/sh
# Strip Cursor agent attribution trailers (enable: git config core.hooksPath .githooks)
sed -e '/cursoragent@cursor\.com/d' \
    -e '/Co-authored-by: Cursor/d' \
    -e '/Made-with: Cursor/d' \
    "$1" > "$1.tmp" && mv "$1.tmp" "$1"
